import { Routes } from '@angular/router';
import { Clima } from './pages/clima/clima';

export const routes: Routes = [
   {
      path: "",
      component: Clima,
      title: "Informaçõaes do clima"
   }

];
